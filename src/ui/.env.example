VITE_API_URL=http://localhost:8010

# LAN example (replace with the IP address or hostname of the machine running
# the API). The browser must be opened from the same origin configured in
# TRANSCRIBE3_CORS_ORIGINS in the project-root .env file.
# VITE_API_URL=http://192.168.1.50:8010
